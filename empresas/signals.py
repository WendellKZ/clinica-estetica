"""Signal hooks for the empresas app.

The current data model does not include per-user company profiles, so this
module intentionally has no receivers. It remains importable for older code
that still imports empresas.signals.
"""
