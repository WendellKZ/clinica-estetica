services:
  - type: web
    name: estetica-clinica
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt && python manage.py collectstatic --noinput
    preDeployCommand: python manage.py migrate
    startCommand: python manage.py bootstrap_admin && gunicorn estetica.wsgi:application --bind 0.0.0.0:$PORT
    healthCheckPath: /health/
    autoDeploy: true
    envVars:
      - key: SECRET_KEY
        generateValue: true
      - key: DEBUG
        value: "0"
      - key: ALLOWED_HOSTS
        value: "estetica-clinica.onrender.com"
      - key: CSRF_TRUSTED_ORIGINS
        value: "https://estetica-clinica.onrender.com"
      - key: SECURE_PROXY_SSL_HEADER
        value: "1"
      - key: SECURE_SSL_REDIRECT
        value: "1"
      - key: SECURE_HSTS_SECONDS
        value: "31536000"
      - key: DATABASE_URL
        fromDatabase:
          name: estetica-clinica-db
          property: connectionString

databases:
  - name: estetica-clinica-db
    plan: free
